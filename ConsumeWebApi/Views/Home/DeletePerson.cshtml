@model Guid

<h2>Confirm Delete Person</h2>
<p>Are you sure you want to delete this person ?</p>

<div>
    <h4>Title:</h4>
   
</div>

@using (Html.BeginForm("DeletePerson","Home",new {id=Model},FormMethod.Post))
{
    <input type ="Submit" value="Delete" class="btn btn-danger"/>
    <a href="@Url.Action("Index","HomeController")" class="btn btn-secondary">Cancle</a>
}
